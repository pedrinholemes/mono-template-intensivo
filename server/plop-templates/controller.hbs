import { BaseController, Request, Response } from '../infra/Controller'
import { {{name}}Services } from '../services/{{name}}Services'

export class {{name}}Controller extends BaseController {
constructor() {
super()
this.router.post('/', this.create)
}

async create(req: Request, res: Response): Promise<void> {
  try {
  const service = new {{name}}Services()

  const {{name}} = await service.create(req.body)

  res.status(201).json({
  {{name}}
  })
  } catch (e) {
  console.error(e)
  res.status(400).json({ message: e.message })
  }
  }
  }
